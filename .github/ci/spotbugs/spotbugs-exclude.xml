<?xml version="1.0" encoding="UTF-8"?>
<!--
    SpotBugs exclude filter — permanent exclusions for false positives.

    This file is the equivalent of phpstan-baseline.neon. Add <Match> entries
    here to suppress known false positives. To suppress a newly found bug
    class across the whole project, use:

        <Match>
            <Bug pattern="BUG_PATTERN_NAME" />
        </Match>

    To suppress for a specific class:

        <Match>
            <Class name="app.some.ClassName" />
            <Bug pattern="BUG_PATTERN_NAME" />
        </Match>

    Run `gradle spotbugsMain` locally to see the full bug report.
-->
<FindBugsFilter
        xmlns="https://github.com/spotbugs/filter/3.0.0"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="https://github.com/spotbugs/filter/3.0.0 https://raw.githubusercontent.com/spotbugs/spotbugs/master/spotbugs/etc/findbugsfilter.xsd">

    <!--
        Intentional design: application controllers extend the framework's
        controllers and intentionally share their simple name (e.g. app's
        Controller extends Valkyrja's Controller). The shadowing is deliberate.
    -->
    <Match>
        <Bug pattern="NM_SAME_SIMPLE_NAME_AS_SUPERCLASS" />
    </Match>
    <Match>
        <Bug pattern="NM_SAME_SIMPLE_NAME_AS_INTERFACE" />
    </Match>

    <!--
        Intentional design: configuration and data records return their mutable
        list fields directly, mirroring the framework's intentional mutability and
        performance goals. Defensive copies everywhere would contradict that design.
    -->
    <Match>
        <Bug pattern="EI_EXPOSE_REP" />
    </Match>
    <Match>
        <Bug pattern="EI_EXPOSE_REP2" />
    </Match>

</FindBugsFilter>
